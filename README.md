# Aoeyaeng Clothing 👗

เว็บแอปพลิเคชันสำหรับร้านขายเสื้อผ้าออนไลน์ พัฒนาด้วย Node.js และ Express.js เชื่อมต่อฐานข้อมูล MySQL บน Cloud (Aiven)

## 🌟 ฟีเจอร์หลัก (Features)

### 1. ระบบสมาชิก (Authentication)
*   **สมัครสมาชิก (Register)**: ผู้ใช้สามารถสมัครสมาชิกใหม่ได้
*   **เข้าสู่ระบบ (Login)**: ล็อกอินเพื่อเข้าใช้งานฟีเจอร์ต่างๆ
*   **ลืมรหัสผ่าน (Forgot Password)**: ส่งรหัสผ่านชั่วคราวทาง Email (ใช้ Nodemailer)
*   **ออกจากระบบ (Logout)**: จัดการ Session อย่างปลอดภัย

### 2. ระบบจัดการสินค้า (Product System)
*   แสดงรายการสินค้าทั้งหมด (New Collection)
*   แยกหมวดหมู่สินค้า (Categories)
*   หน้าแสดงรายละเอียดสินค้า (รูปภาพ, ราคา, คำอธิบาย)

### 3. ระบบตะกร้าสินค้า (Shopping Cart)
*   เพิ่มสินค้าลงตะกร้า (Add to Cart)
*   ลบสินค้าออกจากตะกร้า
*   คำนวณราคารวมอัตโนมัติ
*   ยืนยันคำสั่งซื้อ (Simple Checkout)

### 4. แดชบอร์ดผู้ใช้ (User Dashboard)
*   ดูข้อมูลส่วนตัว
*   เปลี่ยนรหัสผ่าน (Change Password)

---

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

*   **Backend**: Node.js, Express.js
*   **Database**: MySQL (Aiven Cloud), `mysql2` (Driver)
*   **Frontend**: EJS (Templating Engine), Bootstrap 5 (UI Framework)
*   **Authentication**: `bcrypt` (Hash Password), `express-session`
*   **Email**: `nodemailer`

---

## ⚙️ การติดตั้งและรันโปรเจค (Installation)

1.  **Clone โปรเจค**
    ```bash
    git clone https://github.com/aoeyaeng/web-clothing.git
    cd web-clothing
    ```

2.  **ติดตั้ง dependencies**
    ```bash
    npm install
    ```

3.  **ตั้งค่า Environment Variables**
    สร้างไฟล์ `.env` ใน root directory และใส่ค่าดังนี้:
    ```env
    PORT=3000
    
    # การตั้งค่าฐานข้อมูล (Aiven MySQL)
    DB_HOST=ecommerce001-<your-project>.aivencloud.com
    DB_USER=avnadmin
    DB_PASSWORD=<your-password>
    DB_NAME=defaultdb
    DB_PORT=13270
    DB_CA_CERT="-----BEGIN CERTIFICATE----- ... (Certificate Content) ..."

    # รหัสลับสำหรับ Session
    SESSION_SECRET=your_secret_key_here
    ```

4.  **รันเซิร์ฟเวอร์**
    ```bash
    node server.js
    ```
    เซิร์ฟเวอร์จะทำงานที่ `http://localhost:3000`

---

## 📂 โครงสร้างโปรเจค (Project Structure)

*   `server.js`: ไฟล์หลักสำหรับรัน Server และจัดการ Routes
*   `views/`: ไฟล์ EJS (HTML Templates) สำหรับหน้าเว็บต่างๆ
*   `public/`: ไฟล์ Static เช่น รูปภาพ (`images/`), CSS, JS
*   `import_to_aiven.sh`: สคริปต์สำหรับนำเข้าฐานข้อมูลไปยัง Cloud

---

## 👤 ผู้จัดทำ
**Sirikorn Chaowacharoen**
