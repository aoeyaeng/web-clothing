<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register - Aoeyaeng Clothing</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Marcellus&family=Kanit:wght@300;400;500;700&display=swap" rel="stylesheet">
    <style>
        /* 💥 แก้ไข: กำหนดฟอนต์หลัก */
        body {
            font-family: 'Kanit', sans-serif; 
            font-weight: 300; 
            color: #333;
        }
        
        /* 💥 แก้ไข: CSS สำหรับ Navbar (ถ้ามี) */
        .navbar-custom {
          background: linear-gradient(135deg, #fbc2eb, #a6c1ee);
          padding: 15px 0;
        }
        .navbar-custom .navbar-brand {
            color: #fff;
            font-family: 'Marcellus', serif; 
            font-weight: bold;
            font-size: 26px; 
        }
        .navbar-custom .nav-link {
            color: #fff !important;
            margin: 0 10px;
            font-weight: 500;
            font-size: 20px; 
        }

        .card-custom {
            border: none;
            border-radius: 15px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            background-color: #ffffff;
        }
        .section-title {
            color: #ff8fab;
            /* 💥 แก้ไข: ใช้ Marcellus สำหรับหัวข้อ */
            font-family: 'Marcellus', serif; 
            font-weight: 400;
            font-size: 2.3rem;
            text-align: center;
        }
        .btn-custom {
            background: linear-gradient(45deg, #fbc2eb, #a6c1ee);
            border: none;
            color: white;
            font-weight: bold;
            border-radius: 25px;
            padding: 8px 20px;
            transition: background 0.3s ease;
        }
        .btn-custom:hover {
            background: linear-gradient(45deg, #ff9a9e, #fad0c4);
            color: white;
        }
        .link-pastel {
            color: #a6c1ee;
            transition: color 0.3s ease;
        }
        .link-pastel:hover {
            color: #ff8fab;
        }
    </style>
</head>
<body class="bg-light">

<div class="container d-flex justify-content-center align-items-center py-5">
    <div class="card card-custom p-4" style="width: 100%; max-width: 600px;">
        <h2 class="section-title mb-4">New Member Registration</h2>
        
        <% if (error) { %>
            <div class="alert alert-danger" role="alert"><%= error %></div>
        <% } %>

        <form action="/register" method="POST">
          
            <div class="mb-3">
                <label for="fullname" class="form-label">Full Name</label>
                <input type="text" class="form-control" id="fullname" name="fullname" required>
            </div>
            <div class="mb-3">
                <label for="dob" class="form-label">Date of Birth</label>
            
                <input type="date" class="form-control" id="dob" name="dob">
            </div>
            <div class="mb-3">
                <label for="phone" class="form-label">Phone Number</label>
                <input type="tel" class="form-control" id="phone" name="phone">
            </div>
            <div class="mb-3">
   
                 <label for="email" class="form-label">Email (Used as Username)</label>
                <input type="email" class="form-control" id="email" name="email" required>
            </div>
            <div class="mb-3">
                <label for="password" class="form-label">Password</label>
                <input type="password" 
                class="form-control" id="password" name="password" required>
            </div>
            <div class="mb-3">
                <label for="address" class="form-label">Address</label>
                <textarea class="form-control" id="address" name="address" rows="3"></textarea>
            </div>
            
          
            <button type="submit" class="btn btn-custom w-100 mb-3">Register</button> </form>
        
        <div class="text-center mt-3">
            <p>Already have an account?
            <a href="/login" class="link-pastel">Login here</a></p>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>