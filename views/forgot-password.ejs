<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password - Aoeyaeng Clothing</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Marcellus&family=Kanit:wght@300;400;500;700&display=swap" rel="stylesheet">
    <style>
        /* 💥 แก้ไข: กำหนดฟอนต์หลัก */
        body {
            font-family: 'Kanit', sans-serif; 
            font-weight: 300; 
            color: #333;
        }
        
        /* 💥 แก้ไข: CSS สำหรับ Navbar (ถ้ามี) */
        .navbar-custom {
          background: linear-gradient(135deg, #fbc2eb, #a6c1ee);
          padding: 15px 0;
        }
        .navbar-custom .navbar-brand {
            color: #fff;
            font-family: 'Marcellus', serif; 
            font-weight: bold;
            font-size: 26px; 
        }
        .navbar-custom .nav-link {
            color: #fff !important;
            margin: 0 10px;
            font-weight: 500;
            font-size: 20px; 
        }

        .card-custom {
            border: none;
            border-radius: 15px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            background-color: #ffffff;
        }
        .section-title {
            color: #ff8fab;
            /* 💥 แก้ไข: ใช้ Marcellus สำหรับหัวข้อ */
            font-family: 'Marcellus', serif; 
            font-weight: 400;
            font-size: 2.3rem;
            text-align: center;
        }
        .btn-custom {
            background: linear-gradient(45deg, #fbc2eb, #a6c1ee);
            border: none;
            color: white;
            font-weight: bold;
            border-radius: 25px;
            padding: 8px 20px;
            transition: background 0.3s ease;
        }
        .btn-custom:hover {
            background: linear-gradient(45deg, #ff9a9e, #fad0c4);
            color: white;
        }
        .link-pastel {
            color: #a6c1ee;
            transition: color 0.3s ease;
        }
        .link-pastel:hover {
            color: #ff8fab;
        }
        
        .btn-warning-pastel {
            background-color: #ffd1a0;
            border: none;
            color: #333;
            font-weight: bold;
            border-radius: 25px;
            transition: background-color 0.3s ease;
        }
        .btn-warning-pastel:hover {
            background-color: #ffc28b;
            color: #333;
        }
    </style>
</head>
<body class="bg-light">

<div class="container d-flex justify-content-center align-items-center" style="min-height: 100vh;">
    <div class="card card-custom p-4 shadow-sm" style="width: 100%; max-width: 400px;">
        <h2 class="section-title mb-4">Forgot Password</h2>
        
        <% if (error) { %>
            <div class="alert alert-danger" role="alert"><%= error %></div>
        <% } %>

        <% if (success) { %>
     
            <div class="alert alert-success" role="alert"><%= success %></div>
        <% } %>

        <form action="/forgot-password" method="POST">
            <div class="mb-3">
                <label for="email" class="form-label">Enter your registered email</label>
                <input type="email" class="form-control" id="email" name="email" required>
            </div>
 
            <button type="submit" class="btn btn-warning-pastel w-100 mb-3">Get Temporary Password via Email</button>
        </form>
        
        <div class="text-center mt-3">
            <p><a href="/login" class="link-pastel">Back to Login</a></p>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>